# Tarea 9 — Crear mediciones estándar

Estado: completada.

Se creó `qkd/primitives/measurements.py` con proyectores y mediciones reutilizables Z, X e Y. También se añadieron los estados propios Y `PLUS_I` y `MINUS_I`, y el mapa `MEASUREMENTS_BY_BASIS` usa claves `Basis`.

Las instancias son completas, bidimensionales, prevalidadas, de solo lectura y comparten la convención lógica `(0, 1)`.
