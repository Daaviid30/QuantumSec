# Tarea 16 — Pruebas de información cuántica

Estado: completada.

La pureza se contrasta con `1`, `1/2` y `1/4`; la distancia de traza con estados iguales, ortogonales y simetría; la fidelidad con igualdad, ortogonalidad, simetría y solapamiento puro `1/2`; y la entropía con `0`, `1` y `2` bits.

Se añadieron además defensas para formas incompatibles y negatividad significativa cuando se desactiva la validación completa.
