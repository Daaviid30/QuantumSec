# Tarea 18 — Alias de tipos NumPy

Estado: completada.

Se creó `quantum/types.py` con `ArrayLike`, `ComplexArray` y `RealArray`. Las APIs nuevas y refactorizadas los utilizan para distinguir entradas convertibles de matrices y vectores ya materializados.

No se introdujeron clases envoltorio ni se alteró el modelo numérico basado en NumPy.
