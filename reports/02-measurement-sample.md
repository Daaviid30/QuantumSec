# Tarea 2 — Añadir `MeasurementSample`

Estado: completada.

Se creó la dataclass inmutable y con slots `MeasurementSample`, con los campos `index`, `outcome` y `probability`. Se utiliza como resultado ligero de la ruta de muestreo y no contiene ni construye un estado posterior.

Las pruebas cubren el índice físico seleccionado, el resultado lógico asociado y la probabilidad de Born devuelta.
